<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.properity.mapper.CommunityMapper">

    <!--<update id="update">
        update community
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            <if test="genre != null">
                genre = #{genre},
            </if>
            <if test="developer != null">
                developer = #{developer},
            </if>
            <if test="era != null">
                era = #{era},
            </if>
            <if test="quantity != null">
                quantity = #{quantity},
            </if>
            <if test="area != null">
                area = #{area}
            </if>
        </set>
        <where>
            id = #{id}
        </where>
    </update>-->
</mapper>